import React from 'react';
import { Page, Navbar, Block, BlockTitle } from 'framework7-react';

const AboutPage = () => (
	<Page name="about">
		<BlockTitle>AboutPage is working</BlockTitle>
	</Page>
);

export default AboutPage;
